server:
  port: ${CUSTOM.SERVICE.PORT:9080}

eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
encrypt:
  key: ecorth